#
# Executes commands at the start of an interactive session.
#
# Authors:
#   Sorin Ionescu <sorin.ionescu@gmail.com>
#

source ~/.work.sh

# Source Prezto.
if [[ -s "${ZDOTDIR:-$HOME}/.zprezto/init.zsh" ]]; then
  source "${ZDOTDIR:-$HOME}/.zprezto/init.zsh"
fi

# A command to launch a new, anonymous Chrome session for testing.
chrome() {
  mkdir -p /tmp/chrome$1
  ~/Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome --user-data-dir=/tmp/chrome$1
}

# Load fzf
[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh


unsetopt AUTO_NAME_DIRS

# Source aliases
source ~/.aliases.sh

# # The next line updates PATH for the Google Cloud SDK.
# source '/Users/acerna/google-cloud-sdk/path.zsh.inc'

# # The next line enables shell command completion for gcloud.
# source '/Users/acerna/google-cloud-sdk/completion.zsh.inc'

export PATH="$PATH:$HOME/.rvm/bin" # Add RVM to PATH for scripting
